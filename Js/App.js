import { initForm, tampilkanDaftar, tampilkanTotal } from './ui.js';

// Inisialisasi saat halaman dimuat
document.addEventListener('DOMContentLoaded', () => {
  initForm();
  tampilkanDaftar();
  tampilkanTotal();
});
